# PyTorch installed separately via the CUDA wheel index — see README.
# torch  / torchaudio

qwen-asr==0.0.6
transformers==4.57.6
peft==0.19.1
tokenizers>=0.20
accelerate>=0.34
safetensors>=0.4.5

# Audio + data
soundfile
librosa
datasets
pyarrow
tqdm
numpy
scipy

# Eval
jiwer
spacy

# Training infra
wandb

# Forced alignment for endpoint labels
whisperx
