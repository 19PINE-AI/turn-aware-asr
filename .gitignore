__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.venv/
venv/

# Training artifacts (Phase 0+)
checkpoints/
runs/
wandb/
*.ckpt
*.safetensors
*.bin

# Pre-tokenized data and audio caches
data/
cache/
*.arrow
*.parquet
*.tar
*.tar.gz

# Eval outputs (re-runnable, large)
eval/results/

# Editor / OS
.DS_Store
.vscode/
.idea/

# merged inference checkpoints (large, re-derivable via scripts/merge_endpoint_lora.py)
checkpoints/merged/

# external-baseline venvs (exp-4) — large, re-creatable per eval/external/README.md
.venv_ext_*/

# Local credentials and publication correspondence
.env
.env.*
!.env.example
paper/arxiv_moderation_email.txt
